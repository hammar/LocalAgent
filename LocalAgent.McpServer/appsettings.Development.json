{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // For Microsoft To-Do tools authentication:
  // The application uses DefaultAzureCredential which automatically detects:
  // 1. Azure CLI (run 'az login')
  // 2. Azure PowerShell (run 'Connect-AzAccount')
  // 3. Visual Studio (sign in through Tools → Options → Azure Service Authentication)
  // 4. VS Code (sign in through the Azure Account extension)
  // No configuration required - just sign in using one of the above methods
}
