{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // For local development with Microsoft To-Do tools:
  // 1. Run 'az login' to authenticate with Azure CLI, OR
  // 2. Sign in to Visual Studio/VS Code with your Azure account
  // The application will automatically use DefaultAzureCredential to access Microsoft Graph
  // No need to configure TenantId, ClientId, or ClientSecret for local development
}
